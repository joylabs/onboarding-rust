pub fn plus_one(input: Vec<i8>) -> Vec<i8> {
   let mut output: Vec<i8> = vec![];
   let length = input.len() - 1;
   let carry_on = true;

   for i in length..=0 {
      if carry_on {}
   }

   output
}