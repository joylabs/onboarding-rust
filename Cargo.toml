[package]
name = "onboarding-rust"
version = "0.1.0"
authors = ["joylabs"]
edition = "2018"

[dependencies]
regex           = "1"
serde_json      = "1.0"

[dev-dependencies]
criterion = "0.2"

[profile.release]
debug = true
